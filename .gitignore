# Packed archives and pack metadata — built by CI, never committed.
dist/

# Signing keys must never land in the repo. The secret key lives offline and in
# the `publish` environment; only publisher.pub belongs here.
*.key
*.secret

.DS_Store
